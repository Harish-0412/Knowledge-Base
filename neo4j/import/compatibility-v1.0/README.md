# Compatibility v1.0 Neo4j Package

This directory is governed by `scripts/build_phase11_neo4j_package.py`.

The current package is blocked until Phase 9 human decisions and the Phase 10 approved release are complete. A readiness report may exist without CSV or Cypher import artifacts; that is intentional and must not be treated as an importable package.

When unblocked, verify `import_manifest.json`, stage the import first, and follow `docs/CompatibilityNeo4jConnectionGuide.md`.
